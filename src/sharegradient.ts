import { createShareGradient, ShareGradientBuilder } from "./api/ShareGradientBuilder";
import { ShareGradientOptions, GradientType, AnimationMode } from "./core/types";

export { createShareGradient, ShareGradientBuilder };
export type { ShareGradientOptions, GradientType, AnimationMode };
